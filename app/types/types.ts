//** types */
export type NavItem = {
    name: string;
    description: string;
};

//** enums */
export enum PATHS {
    home = "/home",
    about = "/about",
    curriculum = "/curriculum",
    inspirations = "/inspirations",
}

export enum CATEGORIES {
    trees = "Arbres",
    beasts = "Bestiaires",
    illusions = "Illusions",
}
